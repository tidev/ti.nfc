#!groovy
library 'pipeline-library'

buildModule {
	// defaults:
	//nodeVersion = '4.7.3' // Must have version set up on Jenkins master before it can be changed
	sdkVersion = '9.1.0.v20200214034612'
	androidAPILevel = '25' // if changed, must install on build nodes
}
